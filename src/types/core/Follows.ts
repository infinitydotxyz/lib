export type CollectionFollow = {
  userAddress: string;
  address: string;
  chainId: string;
  name: string;
  slug: string;
};

export type UserFollow = {
  userAddress: string;
  address: string;
  name: string;
};
